class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        m = None
        count = 0
        for num in nums:
            if count == 0:
                m = num
            count += 1 if num == m else -1
        return m
